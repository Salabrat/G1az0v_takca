# 🗺️ Настройка Yandex MapKit API

## Получение API ключа

### Шаг 1: Регистрация
1. Перейдите на [developer.tech.yandex.ru](https://developer.tech.yandex.ru/)
2. Войдите через Яндекс ID или зарегистрируйтесь

### Шаг 2: Создание проекта
1. В личном кабинете нажмите **"Подключить API"**
2. Выберите **"JavaScript API и HTTP Геокодер"** или **"MapKit для мобильных"**
3. Укажите название проекта: `Такси Глазов`

### Шаг 3: Получение ключа
1. После создания проекта скопируйте **API ключ**
2. Ключ будет выглядеть примерно так: `a1b2c3d4-e5f6-7890-abcd-ef1234567890`

### Шаг 4: Установка ключа в приложение
1. Откройте файл: `app/src/main/java/com/example/taxi_application/utils/MapKitManager.kt`
2. Найдите строку:
   ```kotlin
   private const val YANDEX_MAPKIT_API_KEY = "YOUR_YANDEX_MAPKIT_API_KEY"
   ```
3. Замените `YOUR_YANDEX_MAPKIT_API_KEY` на ваш реальный ключ:
   ```kotlin
   private const val YANDEX_MAPKIT_API_KEY = "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
   ```

### Шаг 5: Пересборка приложения
```bash
./gradlew clean assembleDebug
```

## ⚠️ Важно

- **НЕ коммитьте API ключ в Git!** Добавьте его в `.gitignore` или используйте переменные окружения
- Бесплатный лимит: 25,000 запросов в день
- Для production используйте ограничения по домену/bundle ID

## Fallback режим

Если API ключ не установлен, приложение автоматически переключится на **Canvas-карту** (GlazovMapView) с предупреждением:

> ⚠️ Yandex MapKit: Не установлен API ключ  
> Используется Canvas-карта

## Проверка работы

После установки ключа:
1. Запустите приложение
2. Войдите с номером `+7 9997770901`
3. На главном экране должна отобразиться реальная карта Глазова
4. Проверьте, что карта интерактивна (масштабирование, перемещение)

## Альтернативные решения

### Вариант 1: Переменные окружения (рекомендуется)
```kotlin
// MapKitManager.kt
private const val YANDEX_MAPKIT_API_KEY = BuildConfig.YANDEX_API_KEY

// build.gradle.kts
android {
    defaultConfig {
        buildConfigField("String", "YANDEX_API_KEY", "\"${System.getenv("YANDEX_API_KEY")}\"")
    }
}
```

### Вариант 2: local.properties
```properties
# local.properties (не коммитится в Git)
yandex.api.key=ваш_ключ_здесь
```

```kotlin
// build.gradle.kts
val localProperties = Properties()
localProperties.load(FileInputStream(rootProject.file("local.properties")))

android {
    defaultConfig {
        buildConfigField("String", "YANDEX_API_KEY", "\"${localProperties["yandex.api.key"]}\"")
    }
}
```

## Полезные ссылки

- [Документация Yandex MapKit](https://yandex.ru/dev/maps/mapkit/)
- [Примеры использования](https://github.com/yandex/mapkit-android-demo)
- [Тарифы и лимиты](https://yandex.ru/dev/maps/commercial/)
